spring:
  datasource:
    url: jdbc:mysql://localhost:3306/sfg_dev
    # TODO add a new user/ avoid using root
    username: root
    password: root
    platform: mysql
  jpa:
    hibernate:
      ddl-auto: validate
    database-platform: org.hibernate.dialect.MySQL5InnoDBDialect
    database: mysql
    show-sql: true
#    properties:
#      javax:
#        persistence:
#          schema-generation:
#            create-source: metadata
#            scripts:
#              action: create
#              create-target: auto_generated_database_schema.sql